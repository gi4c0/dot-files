{
    "configurations": {
        "run": {
            "adapter": "vscode-go",
            "configuration": {
                "request": "launch",
                "stopAtEntry": true,
                "program": "${workspaceRoot}/cmd/main.go",
                "mode": "debug",
                "dlvToolPath": "$HOME/go/bin/dlv"
            }
        }
    }
}
